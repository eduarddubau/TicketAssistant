/**
 * Where the API lives. In dev the SPA is served from :4200 and the API from :5080 (CORS on the
 * API already allows this origin). Override at build/serve time by editing this one constant.
 */
export const API_BASE = 'http://localhost:5080';
