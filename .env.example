# Copy to .env (gitignored) and override as needed. docker compose reads .env automatically.

# Which LLM backend the assistant uses:
#   Ollama    - local, no API key (default)
#   Anthropic - Claude models
#   OpenAI    - GPT models
#   Google    - Gemini models (via OpenAI-compatible endpoint)
LLM_PROVIDER=Ollama

# Ollama model (used when LLM_PROVIDER=Ollama). Pull it once with:
#   docker compose exec ollama ollama pull llama3.2:3b
# qwen3:4b / qwen3:8b are heavier alternatives with stronger multi-step tool calling.
OLLAMA_MODEL=llama3.2:3b

# Hosted-provider API keys — only the key for the selected LLM_PROVIDER is needed.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
